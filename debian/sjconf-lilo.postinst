#!/bin/sh
set -e

#DEBHELPER#

PLUGINS="lilo"

for plugin in $PLUGINS
do
	if ! sjconf --list-plugins | grep "^Plugin $plugin$" > /dev/null
	then
		sjconf --install-conf --link /usr/share/sjconf-lilo/confs/$plugin.conf
		sjconf --install-plugin --link /usr/share/sjconf-lilo/plugins/$plugin.py
		sjconf --enable-plugin $plugin
	fi
done
